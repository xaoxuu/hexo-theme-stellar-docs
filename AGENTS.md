# AGENTS.md — Stellar 公开文档协作规范

本文件拥有 hexo-theme-stellar-docs 的内容维护与交付规则，适用于本仓库文件。公开文档默认使用简体中文。

## 范围与版本

- 本仓库负责公开 Wiki 正文、示例、链接与资源引用；通用主题实现及其工程门禁归主题仓库，消费站点的配置、导航数据、构建部署和 gitlink 归消费站点。
- 修改行为说明前确认目标主题版本，从该版本的默认配置、源码、Schema 或已发布 CHANGELOG 核实事实。未发布行为只有任务明确包含时才写入，并标明适用版本。
- 在消费站点的子模块中工作时，分别读取主题或站点的 AGENTS.md 后才修改其文件；本仓库可独立检出，不依赖固定的宿主目录。

## 内容维护

- 保持页面既有 Front Matter、Stellar 标签语法与示例结构；新页面参照同类页面的 `title` 和 `collection`，导航数据由消费方维护。
- 正文标题首先服务内容表达，使用 Markdown 标题语法；调整标题时同步当前文档内链，并以消费站点生成结果为准。`redirects.json` 属于 URL 迁移分支，只记录旧页面到新页面的路径映射；普通正文任务以当前内链和生成页面为验收范围。
- Markdown 表格的每个分隔列使用 `:---` 显式左对齐，代码示例中的表格保持相同约定。
- 页面 `date` 表示首次创建时间，既有页面保留；新页面填写当前时间。正文变化时刷新 `updated`，仅修改标题或 Front Matter 时保持原值。格式统一为 `YYYY-MM-DD HH:mm`（Asia/Shanghai）；本文件等工程规范不添加内容时间字段。
- 页面 URL 取决于消费方路径与路由配置。重命名、移动或删除页面属于 URL 迁移，按消费方流程核对原 URL、目标 URL、内链、导航与必要重定向。
- 文案准确说明功能和适用版本；修改配置示例时核对字段、默认值和所属层级。图片沿用既有资源约定。
- 长期门禁保护页面可访问性、可解析格式和已发布外部契约；标题、文案、章节顺序、示例与当前排版方案按本次内容和渲染验收，保持后续可直接编辑。
- 说明当前支持范围时先建立**覆盖矩阵**：从目标版本的 Schema 枚举、注册表、默认配置及实际消费者提取全部内置成员，并为每一项记录名称、用途、前置条件、最小可用配置和参数边界。运行时允许扩展时，将“内置完整清单”与“可扩展的上游参数或自定义实现”分别说明；“例如”“等”只用于完整清单之后的补充示例，不代替清单。

## 验证与交付

- 先记录本仓库起始工作树状态，保留无关修改。普通文案检查 Front Matter、标签闭合、链接与事实；配置示例增加目标版本解析检查。
- 修改能力说明时逐项核对覆盖矩阵：文档列出的内置成员与权威源集合一致，每个成员都有可执行的使用入口，默认值、上下文限制和空内容条件与消费者一致。集合差异或缺少使用说明均表示检查未完成；页面生成后再核对目录、锚点和代码块。
- 影响标签渲染、路由或导航，或任务要求页面验收时，在已有消费站点按其规范构建并抽查受影响页面。独立检出缺少渲染环境时说明已完成检查与剩余验证；不虚报构建通过。
- 将本仓库挂载到 Hexo `source/` 的消费方须排除 `AGENTS.md` 等工程入口，避免它们成为公开页面。
- 默认保留工作区改动；用户要求提交才本地提交，要求推送才推送。使用 Conventional Commits（正文 `content`、工程规范 `docs`），只提交获准改动；宿主 gitlink 由宿主另行记录。
- 有 Issue 时可读取上下文；创建、评论或修改状态须有对应用户授权，已有授权在本任务范围内持续有效。获准持久化的方案与验收记录进入本仓库 GitHub Issues，跨仓库使用完整链接；无写入授权时在对话中交付，不阻断本地工作。

完成条件：本次内容与目标版本一致；涉及能力清单时集合差异为空且每项用法齐全；适用检查通过。提交、推送和宿主引用仅按授权执行，并明确报告未完成的验证或集成项。
